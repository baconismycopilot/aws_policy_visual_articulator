{
  "name": "aws-policy-visual-articulator",
  "private": true,
  "type": "module",
  "description": "Dev-only. The site itself ships no npm dependencies.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
